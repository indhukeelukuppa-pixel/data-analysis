# data-analysis
This code performs Exploratory Data Analysis (EDA) on the Titanic dataset using Python libraries such as Pandas, Matplotlib, and Seaborn. It loads the dataset, displays the first five records, checks dataset information, identifies missing values, and generates summary statistics. 
